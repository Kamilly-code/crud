spring.application.name=crud
server.port=${PORT:4000}

# Configurações do banco (sobrescritas por variáveis de ambiente)
spring.datasource.url=${DATABASE_URL:jdbc:postgresql://dpg-d14k7te3jp1c73bgpokg-a.oregon-postgres.render.com:5432/clouddydatabase}
spring.datasource.username=${DATABASE_USERNAME:clouddy}
spring.datasource.password=${DATABASE_PASSWORD:m1vNW1mgCWTl5Z1874j1aHoFOnfSYwLK}

# Configurações JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.jdbc.batch_size=10
spring.jpa.properties.hibernate.order_inserts=true

# Otimização para reduzir memória
spring.main.lazy-initialization=true
spring.jmx.enabled=false

# Configurações de segurança
app.jwtSecret=${JWT_SECRET:yourSecretKey}
app.jwtExpirationMs=${JWT_EXPIRATION:86400000}

