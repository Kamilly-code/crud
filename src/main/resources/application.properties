spring.application.name=crud
server.port=${PORT:4000}

# Configurações do banco de dados
spring.datasource.url=${DATABASE_URL:jdbc:postgresql://yamanote.proxy.rlwy.net:33909/railway}
spring.datasource.username=${DATABASE_USERNAME:postgres}
spring.datasource.password=${DATABASE_PASSWORD:PBePqAltHfEwiGBVZfKAArZKznbBIDTN}

# Configurações JPA/Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.jdbc.batch_size=10
spring.jpa.properties.hibernate.order_inserts=true

# Otimizações
spring.main.lazy-initialization=true
spring.jmx.enabled=false

# Segurança/JWT
app.jwtSecret=${JWT_SECRET:yourSecretKey}
app.jwtExpirationMs=${JWT_EXPIRATION:86400000}

